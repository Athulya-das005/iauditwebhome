/**
 * Email OTP verification before starting self-assessment / gap analysis.
 *
 * Currently OFF so users can start after filling the form (no Verify step).
 * Set to `true` when you want to re-enable email codes in the start modal
 * and the matching check in `/api/assessment-leads`.
 */
export const REQUIRE_ASSESSMENT_EMAIL_OTP = false;
