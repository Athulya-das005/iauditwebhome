/**
 * DRAFT — SafetyCulture comparison page is hidden from the live site.
 *
 * To re-enable:
 * 1. Delete this file and rename `page.draft.tsx` back to `page.tsx`
 * 2. Uncomment the Comparison nav item in `src/components/Header.tsx`
 */
import { notFound } from "next/navigation";

export default function ComparisonPage() {
    notFound();
}
